{
  "name": "store-art",
  "private": true,
  "type": "module",
  "description": "Render store screenshots from a manifest with HTML/CSS styles via Playwright",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "render": "node scripts/render.mjs"
  },
  "dependencies": { "playwright": "^1.50.0" }
}
